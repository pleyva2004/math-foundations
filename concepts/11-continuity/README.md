# Continuity

**Level:** `advanced`
**Concept ID:** `11-continuity`
**Prerequisites:** [10-sequences-and-limits](../10-sequences-and-limits/README.md)
**Used by:** [12-derivatives](../12-derivatives/README.md), [15-integration](../15-integration/README.md), [30-existence-uniqueness](../30-existence-uniqueness/README.md)

## Plain-English intro
The epsilon-delta definition; continuity as preservation of limits; uniform vs pointwise continuity.

## 📖 Symbols you'll see in this lesson

| Symbol | LaTeX | Read aloud | Meaning |
|---|---|---|---|
| ε | `\epsilon` | epsilon | epsilon (small positive tolerance on the output side) |
| δ | `\delta` | delta | delta (small positive tolerance on the input side) |
| ⇒ | `\implies` / `\Rightarrow` | implies | logical implication: if LHS then RHS |
| ∈ | `\in` | is an element of / in | membership of an element in a set |
| → | `\to` | tends to / approaches | limit / function arrow (e.g. $x_n \to c$) |
| ℝ | `\mathbb{R}` | the reals | the real numbers |
| ℕ | `\mathbb{N}` | the natural numbers | the natural numbers $\{1, 2, 3, \dots\}$ |
| ⊆ | `\subseteq` | is a subset of (or equal) | subset (possibly equal) |
| ∘ | `\circ` | composed with | function composition $g \circ f$ |
| ≤ | `\leq` | less than or equal to | non-strict inequality |
| ≥ | `\geq` | greater than or equal to | non-strict inequality |
| $d(x,y)$ | `d(x, y)` | distance from x to y | metric (distance) on a metric space |

## Formal definition
$$
\text{See hand-written content below.}
$$

## Why this matters
_(See below.)_

## Code
The aligned runnable demo lives at [`code.py`](./code.py). CPU-runnable, <30 s.

## Lesson PDF
See [`lesson.pdf`](./lesson.pdf) once CI has rendered it; source is [`lesson.tex`](./lesson.tex).

## Interactive notebook
See [`notebook.ipynb`](./notebook.ipynb) — open in Colab/Jupyter for cell-by-cell execution.

## Try it yourself
_(See below.)_

## Further reading
_(See below.)_

<!-- ⬇️  Below this line: hand-written content. Above: auto-generated by generate.py — do not edit. -->

<!-- Add hand-written content below: full plain-English exposition, formal
     definition body, worked examples, theorems, why-it-matters paragraph
     tying to specific papers, exercises, references. -->
