"""Optimal Transport (Wasserstein) — concept 41-optimal-transport of the math-foundations learning map.

Wasserstein distance W_p(mu, nu) = inf over couplings of E[|X - Y|^p]^(1/p); the cost of optimally moving one distribution to another. Provides a meaningful metric on distributions even when they have disjoint supports.

This is the auto-generated skeleton. Replace the body of main() with a
concrete, CPU-runnable demonstration of the concept (≤30 s, ≤80 lines).
For abstract concepts, implement a finite/discrete witness.
"""


def main():
    print("Optimal Transport (Wasserstein) (concept 41-optimal-transport)")
    print("=" * 60)
    print("TODO: implement a runnable demonstration of this concept.")
    print()
    print("See ../concepts/41-optimal-transport.md for the formal definition.")


if __name__ == "__main__":
    main()
